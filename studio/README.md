# FitGround 技术设计师工作台

Next.js 界面，回答「下一版样衣该改什么」。数据来自 `public/data/workspace.json`（从 `artifacts/demo/workspace.json` 复制），不是聊天机器人。

页面右上角可 **一键切换 中文 / English**。

```bash
npm install
npm run dev
# http://127.0.0.1:43187
```

English: the header toggle switches the whole workspace copy. Same URL.

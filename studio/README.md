# FitGround Technical Designer Workspace

Next.js UI for counterfactual fit correction. Driven by `public/data/workspace.json` (copied from `artifacts/demo/workspace.json`), not a chatbot.

```bash
npm install
npm run dev
# http://127.0.0.1:43187
```
